RM = rm

C_OBJS = blassoc.o
FT_OBJS = blasso_koe.o plqdf1.o

all:
	@$(MAKE) $(SHLIB)
	@rm -f *.mod *.o

clean:	
	$(RM) *.mod *.o  
	echo Clean done

$(SHLIB): $(FT_OBJS) $(C_OBJS)

blassoc.c: blasso_koe.o



