RM = rm

C_OBJS = cassoc.o
FT_OBJS = kaikki.o plqdf1.o
PKG_FCFLAGS = -fopenmp
PKG_LIBS = -fopenmp

all:
	@$(MAKE) $(SHLIB)
	@rm -f *.mod *.o

clean:	
	$(RM) *.mod *.o  
	echo Clean done

$(SHLIB): $(FT_OBJS) $(C_OBJS)

cassoc.c: kaikki.o

